<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>TRAK - Driver Expense Tracker</title>

  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #f4f9ff;
      color: #222;
    }

    header {
      background: #0d6efd;
      padding: 15px;
      text-align: center;
      color: white;
      font-size: 20px;
      font-weight: bold;
    }

    .container {
      padding: 20px;
      max-width: 420px;
      margin: auto;
    }

    .screen {
      display: none;
    }

    .active {
      display: block;
    }

    h2 {
      text-align: center;
      margin-bottom: 10px;
    }

    .tagline {
      text-align: center;
      font-size: 14px;
      color: #444;
      margin-bottom: 20px;
    }

    input, select {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border-radius: 10px;
      border: 1px solid #ccc;
      font-size: 16px;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: 12px;
      background: #0d6efd;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background: #084298;
    }

    .role-box {
      display: flex;
      justify-content: space-between;
      gap: 15px;
      margin-top: 25px;
    }

    .role-option {
      flex: 1;
      background: white;
      border-radius: 15px;
      padding: 25px 10px;
      text-align: center;
      font-size: 18px;
      font-weight: bold;
      cursor: pointer;
      border: 2px solid #0d6efd;
      transition: 0.2s;
    }

    .role-option:hover {
      background: #e7f1ff;
    }

    .helper {
      font-size: 14px;
      text-align: center;
      margin-top: 10px;
      color: #333;
    }

    .card {
      background: white;
      padding: 15px;
      border-radius: 15px;
      margin: 15px 0;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .stats {
      display: flex;
      justify-content: space-between;
      gap: 10px;
    }

    .stat-box {
      flex: 1;
      background: #f1f7ff;
      padding: 12px;
      border-radius: 12px;
      text-align: center;
      font-size: 14px;
    }

    .profit {
      font-weight: bold;
      font-size: 18px;
      color: green;
    }

    .nav-btn {
      background: #222;
      margin-top: 20px;
    }
  </style>
</head>

<body>

  <header>TRAK 🚖 Expense Tracker</header>

  <div class="container">

    <!-- SCREEN 1 LOGIN -->
    <div class="screen active" id="loginScreen">
      <h2>Login / Sign Up</h2>
      <p class="tagline">“உங்கள் தினசரி செலவுகளை கண்காணிக்கவும்”</p>

      <input type="text" placeholder="Phone or Email" />
      <input type="password" placeholder="Password" />

      <button onclick="showScreen('roleScreen')">Login</button>
      <button style="background:#198754;" onclick="showScreen('roleScreen')">Sign Up</button>
    </div>

    <!-- SCREEN 2 ROLE SELECTION -->
    <div class="screen" id="roleScreen">
      <h2>Select Your Role</h2>
      <p class="tagline">Choose your work type</p>

      <div class="role-box">
        <div class="role-option" onclick="showScreen('entryScreen')">
          🚖 Driver
          <p style="font-size:13px;">Auto / Rapido / Taxi</p>
        </div>

        <div class="role-option" onclick="showScreen('entryScreen')">
          🛵 Delivery
          <p style="font-size:13px;">Food / Product Delivery</p>
        </div>
      </div>
    </div>

    <!-- SCREEN 3 DAILY ENTRY -->
    <div class="screen" id="entryScreen">
      <h2>Daily Entry</h2>
      <p class="helper">“இன்றைய செலவு மற்றும் வருமானத்தை பதிவு செய்யவும்”</p>

      <input type="date" />
      <input type="number" placeholder="Earnings ₹" />
      <input type="number" placeholder="Expenses ₹" />

      <select>
        <option>Select Category</option>
        <option>Fuel / Petrol</option>
        <option>Food</option>
        <option>Maintenance</option>
        <option>Mobile Recharge</option>
        <option>Other</option>
      </select>

      <button onclick="showScreen('dashboardScreen')">Save Entry</button>
    </div>

    <!-- SCREEN 4 DASHBOARD -->
    <div class="screen" id="dashboardScreen">
      <h2>Dashboard Summary</h2>

      <div class="stats">
        <div class="stat-box">
          Earnings<br><b>₹4,200</b>
        </div>
        <div class="stat-box">
          Expenses<br><b>₹1,600</b>
        </div>
      </div>

      <div class="card">
        Profit This Week:<br>
        <span class="profit">₹2,600</span>
      </div>

      <div class="card">
        <h3>Daily Expenses Chart</h3>
        <canvas id="expenseChart"></canvas>
      </div>

      <div class="card">
        <h3>Earnings Trend</h3>
        <canvas id="earningChart"></canvas>
      </div>

      <div class="card">
        <h3>Monthly Summary</h3>
        <p>Total Earnings: ₹18,000</p>
        <p>Total Expenses: ₹6,200</p>
        <p><b>Profit: ₹11,800</b></p>
      </div>

      <button class="nav-btn" onclick="showScreen('entryScreen')">
        ➕ Add New Entry
      </button>
    </div>

  </div>

  <script>
    function showScreen(screenId) {
      document.querySelectorAll(".screen").forEach(screen => {
        screen.classList.remove("active");
      });
      document.getElementById(screenId).classList.add("active");
    }

    // Charts
    setTimeout(() => {
      new Chart(document.getElementById("expenseChart"), {
        type: "bar",
        data: {
          labels: ["Mon", "Tue", "Wed", "Thu", "Fri"],
          datasets: [{
            label: "Expenses ₹",
            data: [200, 400, 300, 500, 200]
          }]
        }
      });

      new Chart(document.getElementById("earningChart"), {
        type: "line",
        data: {
          labels: ["Mon", "Tue", "Wed", "Thu", "Fri"],
          datasets: [{
            label: "Earnings ₹",
            data: [800, 900, 750, 1000, 850]
          }]
        }
      });
    }, 500);
  </script>

</body>
</html>
